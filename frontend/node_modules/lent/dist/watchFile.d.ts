import chokidar from 'chokidar';
import { LentHttpInstance, FileCallback, FileUrl } from './types';
export declare const createWatchFile: (l: LentHttpInstance) => chokidar.FSWatcher;
export declare const handleWatchFileEvent: () => {
    on(eventName: string, fileCallback: FileCallback): void;
    emit(eventName: string, path: string): void;
};
export declare const addFileChange: (l: LentHttpInstance, fileUrl: FileUrl, callback: () => void) => void;
